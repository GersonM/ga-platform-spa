import React from 'react';
import {MoveTrip} from '../../../Types/api';

interface PassengerProfileCardProps {
  passenger: MoveTrip;
}

const PassengerProfileCard = ({}: PassengerProfileCardProps) => {
  return <div></div>;
};

export default PassengerProfileCard;
