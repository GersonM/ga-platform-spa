import React from 'react';
import './styles.less';

interface ProcessDetailProps {
  entityUuid: string;
  type: string;
}

const ProcessDetail = ({entityUuid, type}: ProcessDetailProps) => {
  return (
    <div>
      <h3>Documentos</h3>

    </div>
  );
};

export default ProcessDetail;
